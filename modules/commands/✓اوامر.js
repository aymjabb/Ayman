module.exports.config = {
  name: "سيرا_تشان",
  version: "1.0.1",
  hasPermssion: 0,
  credits: "Sera Chan",
  description: "قائمة أوامر البوت سيرا تشان مزخرفة بطابع أنمي",
  commandCategory: "النظام",
  usages: ".سيرا_تشان",
  cooldowns: 5
};

module.exports.run = async function({ api, event }) {
  const { threadID } = event;

  const categories = {
    "🎮 العاب": ["تفكيك", "تجميع"],
    "🖼️ صور": ["مطلوب", "اصفعي", "حضن2"],
    "🛠️ خدمات": ["معلمي", "المطور", "تقرير"],
    "👑 مسؤولي المجموعات": ["كنية", "ايقاف", "تشغيل"],
    "😂 ترفيه": ["اضحك", "مزاح"],
    "📜 النظام": ["اوامر", "سيرا_تشان"]
  };

  let msg = `🌸✨╔═══════════════════════════╗✨🌸\n`;
  msg += `💫      🌟 قائمة أوامر سيرا تشان 🌟      💫\n`;
  msg += `🌸✨╠═══════════════════════════╣✨🌸\n\n`;

  Object.keys(categories).forEach((cat, i) => {
    msg += `💠 فئة: ${cat}\n`;
    categories[cat].forEach((cmd, j) => {
      msg += `   ✧ ${j+1}. ${cmd}\n`;
    });
    msg += `\n`;
  });

  msg += `🌸✨╚═══════════════════════════╝✨🌸\n`;
  msg += `📌 للرد على الفئة أو الرقم لتفعيل الأمر\n`;
  msg += `💻 المطور: سيرا تشان | 2026`;

  return api.sendMessage(msg, threadID);
};
