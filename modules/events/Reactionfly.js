module.exports = function({ api, event }) {
    const DEV_ID = "61577861540407"; // معرف المطور
    const { senderID, threadID, messageID, body } = event;

    // فقط تفاعل مع المطور
    if(senderID !== DEV_ID) return;

    // زخرفة ASCII بالصندوق
    const butterflyMsg = `
╔═════════════════════
║ 🦋 ليـلـى تفاعـلـت! 🦋
╠═════════════════════
║ 🌸 رسالة المطور:
║ "${body}"
║
║ 💫 ليـلـى ترسل لك فراشة جميلة
║ 🦋🌼🦋🌸🦋🌺🦋
╚═════════════════════
    `;

    // إرسال الرسالة مباشرة بدون أي تأخير
    api.sendMessage(butterflyMsg, threadID, messageID);
};
